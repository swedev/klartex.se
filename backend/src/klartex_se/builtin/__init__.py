"""Built-in page templates that ship with the backend.

Each subdirectory is one template: `page_template.json` with its slots and
description, plus the asset files it references. They resolve for every
caller by name, like registered bundles, but live in the package rather
than on the data volume and cannot be written through the API.
"""
